generame una guia paso a paso para crear mi aplicacion de recursos humanos creando el backend con flask, voy a usar mysql como base de datos y windows como sistema operativo, voy a usar pycharm como IDE, y algunas de las dependencias que voy a usar son: flask, flask-restful, sqlalchemy, migrate, pymysql, marshmallow, cors. La estructura de mis archivos es la siguiente: app.py, extensions.py, models.py, schemas.py, carpeta api/ con __init__.py y empleados.py, genera el codigo completo de cada archivo, con explicacion breve de por que se hace asi, las migraciones se van a hacer con alembic (flask db init, migrate, upgrade), la tabla de base de datos que se debe crear es: idEmpleado (autoincrementable), nombre, departamento, sueldo (valor decimal)